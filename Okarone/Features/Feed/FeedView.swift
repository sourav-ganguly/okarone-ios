import SwiftUI

struct FeedView: View {
    var body: some View {
        NavigationView {
            Text("Feed View")
        }
        .background(Color.clear)
    }
}

#Preview {
    FeedView()
} 
